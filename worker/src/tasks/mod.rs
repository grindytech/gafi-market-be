// use crate::workers::Task;
pub mod collection;
pub mod game;
pub mod nft;
pub mod pool;
pub mod trade;

// pub fn create_tasks() -> Vec<Task> {
// 	let mut tasks = vec![];

// 	tasks.append(&mut nft::tasks());
// 	tasks.append(&mut collection::tasks());
// 	tasks.append(&mut pool::tasks());
// 	tasks.append(&mut game::tasks());

// 	tasks
// }
